ctx {
  dataSourceClassName = "org.postgresql.ds.PGSimpleDataSource"
  dataSource {
    url = "jdbc:postgresql://postgres:5432/booking"
    user = "booking"
    password = "booking"
  }
  connectionTimeout = 30000
}

app {
  server {
    host = "0.0.0.0"
    port = 8080
  }
  
  database {
    url = "jdbc:postgresql://postgres:5432/booking" #docker
    # url = "jdbc:postgresql://localhost:5432/booking" #local
    user = "booking"
    password = "booking"
    driver = "org.postgresql.Driver"
    poolSize = 10
  }
} 