app {
  server {
    host = "0.0.0.0"
    port = 8080
  }
  
  database {
    url = "jdbc:postgresql://postgres:5432/booking"
    user = "booking"
    password = "booking"
    driver = "org.postgresql.Driver"
    poolSize = 10
  }
} 